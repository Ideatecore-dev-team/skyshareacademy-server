description: Conflict error (e.g., Data already exists)
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: boolean
          example: false
        errors:
          type: boolean
          example: true
        message:
          type: string
          example: Data already exists